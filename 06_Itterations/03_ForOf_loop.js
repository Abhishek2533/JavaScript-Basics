// FOR OF

const arr = [1, 2, 3, 4, 5];

for (const num of arr) {
    console.log(num);
}

// this will detect starting and ending points automatically ,,,, num is itterator and arr is on which we want to loop

// here, num contains each value of array in each itteration